"use strict";
/***
 *
 *
 * Conditional Tests: Write a series of conditional tests. Print a statement
describing each test and your prediction for the results of each test. Your code
should look something like this:
let car = 'subaru';
console.log("Is car == 'subaru'? I predict True.")
console.log(car == 'subaru')
 *
 *
 */
let course = "web 3.0";
let quater = 1;
let city = "lahore";
let stime = "2pm";
let institute = "piaic";
console.log((course === "web 3.0" ? "predict True" : "Predict False"));
console.log((course === "web 3.o" ? "predict True" : "Predict False"));
console.log((quater === 1 ? "predict True" : "Predict False"));
console.log((quater === 2 ? "predict True" : "Predict False"));
console.log((city === "lahore" ? "predict True" : "Predict False"));
console.log((city === "Lahore" ? "predict True" : "Predict False"));
console.log((stime === "2pm" ? "predict True" : "Predict False"));
console.log((stime === "2 pm" ? "predict True" : "Predict False"));
console.log((institute === "piaic" ? "predict True" : "Predict False"));
console.log((institute === "PIAIC" ? "predict True" : "Predict False"));
